# npm install rebuilds dependencies inside the container
node_modules

# Output of a previous build; regenerated in the build stage
dist

# End-to-end tests take no part in the build
e2e
playwright.config.js
test-results
playwright-report

.env
